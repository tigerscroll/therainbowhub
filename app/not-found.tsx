import { GlobalNotFound } from "@/components/GlobalNotFound";
import "./globals.css";

export const metadata = {
  title: "Page not found - The Rainbow Hub",
};

export default function NotFound() {
  return <GlobalNotFound />;
}
