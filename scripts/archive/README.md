# Archived migrations

These scripts document one-time content and schema migrations. They are not imported by the application or executed by production, validation, or test commands.

`legacy/` contains older content builders, localization repair scripts and theme upgrades. They remain available for historical reference, but must not be run against the current versioned quiz schema without being updated first.
